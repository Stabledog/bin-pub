# taskrc for tox-py

function debug {
    while true; do
        pudb ~/bin/tox-py/tox_core.py "$@"
        read -p "Hit a key to retry"
    done
}

function run {
    $HOME/bin/tox-py/tox_core.py "$@"
}

function reload {
    source $HOME/bin/tox-py/taskrc
}

