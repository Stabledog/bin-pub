#bind-key C-a send-prefix
#unbind-key C-b

# hjkl => change windows like vi

#bind-key C-a send-prefix
#set -g default-terminal "rxvt-xpm"

unbind-key h
unbind-key j
unbind-key k
unbind-key l
bind-key -r     h select-pane -L
bind-key -r     l select-pane -R
bind-key -r     j select-pane -D
bind-key -r     k select-pane -U

