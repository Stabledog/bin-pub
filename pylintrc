# Symlink to ~/.pylintrc
# 

# For trouble importing modules...:

[MASTER]
init-hook="from pylint.config import find_pylintrc;
import os,sys; sys.path.append(os.path.dirname(find_pylintrc()))"

# NOTE: consider running 'pylint --generate-rcfile > .pylintrc' in the project
# home folder first, so the project can override the user defaults.
#
